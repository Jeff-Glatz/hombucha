@TypeDef(name = "measure",
        defaultForType = Measure.class,
        typeClass = MeasureType.class)
package ruffkat.hombucha.measure;

import org.hibernate.annotations.TypeDef;

import javax.measure.Measure;

